# Runtime pins for reproducible worker images.
# Transitive pins are left flexible so they resolve against diffusers main.
runpod==1.11.0
boto3==1.43.66
# torchao>=0.17 imports torch.nn.functional.ScalingType (PyTorch 2.8+/nightly).
# Base image is pytorch 2.7.1 — stay on 0.15.x.
torchao==0.15.0
transformers==5.14.1
accelerate==1.10.1
safetensors>=0.8.0
sentencepiece>=0.2.1
protobuf>=5.0
huggingface_hub>=1.23.0,<2.0
numpy>=1.26
Pillow>=10.0
opencv-python-headless>=4.10
av>=14.0
einops>=0.8
regex>=2025.10.22
tqdm>=4.66
# diffusers is installed separately in the Dockerfile (--force-reinstall --no-deps)
# after this file — a combined resolve with transformers==5.14.1 pinned here was
# silently landing an older diffusers with no ModularPipeline at runtime.
